{
  "name": "geo-visibility",
  "private": true,
  "type": "module",
  "description": "Internal N3 tool: CLI for the AI visibility study (the engine lives in src/lib/geo-visibility; install the root package first).",
  "scripts": {
    "run": "node --env-file-if-exists=.env src/run.ts",
    "analyze": "node src/analyze.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.13.5",
    "typescript": "^7.0.2"
  }
}
